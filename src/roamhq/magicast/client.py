# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.magicast_info import MagicastInfo
from .raw_client import AsyncRawMagicastClient, RawMagicastClient
from .types.list_magicast_response import ListMagicastResponse


class MagicastClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawMagicastClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawMagicastClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawMagicastClient
        """
        return self._raw_client

    def list(
        self,
        *,
        after: typing.Optional[dt.datetime] = None,
        before: typing.Optional[dt.datetime] = None,
        ascending: typing.Optional[bool] = None,
        limit: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListMagicastResponse:
        """
        List Magicasts in your account, most recent first.

        Returns metadata only (`id`, `name`, `createdAt`, `ownerId`,
        `coverImageUrl`). Use [`/magicast.info`](https://developer.ro.am/docs/api/magicast-info) for
        transcript cues, chapters, video status, and a signed download URL.

        **Access:** Organization and Personal. Organization tokens list every
        Magicast in the account, including ones the creator never shared. Personal
        tokens are restricted to Magicasts owned by the authenticated user.

        **Required scope:** `magicast:read`

        Parameters
        ----------
        after : typing.Optional[dt.datetime]
            Only return magicasts created after this time (RFC-3339).

        before : typing.Optional[dt.datetime]
            Only return magicasts created before this time (RFC-3339).

        ascending : typing.Optional[bool]
            Sort oldest-first instead of newest-first.

        limit : typing.Optional[int]
            Number of magicasts to return per response. Default 10.

        cursor : typing.Optional[str]
            Opaque pagination cursor from a previous response's `nextCursor`. Do not construct cursors manually.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListMagicastResponse
            Magicasts retrieved successfully

        Examples
        --------
        from roamhq import RoamClient

        client = RoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )
        client.magicast.list()
        """
        _response = self._raw_client.list(
            after=after, before=before, ascending=ascending, limit=limit, cursor=cursor, request_options=request_options
        )
        return _response.data

    def info(self, *, id: str, request_options: typing.Optional[RequestOptions] = None) -> MagicastInfo:
        """
        Get details for a single Magicast by ID, including transcript cues,
        chapters, video status, a signed video download URL when ready, and a
        player URL if a share link already exists.

        This is the content endpoint. [`/magicast.list`](https://developer.ro.am/docs/api/magicast-list)
        returns metadata only. Magicasts are not meetings — they do not appear on
        [`/recording.list`](https://developer.ro.am/docs/api/recording-list) or meeting transcript
        surfaces, and they have no Magic Minutes summary or action items.

        Asset, transcript, and share-link lookups are best-effort. If the video or
        transcript is still processing, those fields are omitted and the request
        still succeeds. Fetching this endpoint **never** mints a shareable link;
        use [`/magicast.shareLink`](https://developer.ro.am/docs/api/magicast-share-link) for that.

        There is no `https://ro.am/magicast/{id}` browser URL. The player URL is
        always `https://ro.am/share/{key}`.

        **Access:** Organization and Personal. Organization tokens can read every
        Magicast in the account, including ones the creator never shared. Personal
        tokens are restricted to Magicasts owned by the authenticated user. Filter
        on whether `shareUrl` is present if you only want shared recordings.

        **Required scope:** `magicast:read`

        Parameters
        ----------
        id : str
            The magicast ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MagicastInfo
            Magicast retrieved successfully

        Examples
        --------
        from roamhq import RoamClient

        client = RoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )
        client.magicast.info(
            id="id",
        )
        """
        _response = self._raw_client.info(id=id, request_options=request_options)
        return _response.data


class AsyncMagicastClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawMagicastClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawMagicastClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawMagicastClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        after: typing.Optional[dt.datetime] = None,
        before: typing.Optional[dt.datetime] = None,
        ascending: typing.Optional[bool] = None,
        limit: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListMagicastResponse:
        """
        List Magicasts in your account, most recent first.

        Returns metadata only (`id`, `name`, `createdAt`, `ownerId`,
        `coverImageUrl`). Use [`/magicast.info`](https://developer.ro.am/docs/api/magicast-info) for
        transcript cues, chapters, video status, and a signed download URL.

        **Access:** Organization and Personal. Organization tokens list every
        Magicast in the account, including ones the creator never shared. Personal
        tokens are restricted to Magicasts owned by the authenticated user.

        **Required scope:** `magicast:read`

        Parameters
        ----------
        after : typing.Optional[dt.datetime]
            Only return magicasts created after this time (RFC-3339).

        before : typing.Optional[dt.datetime]
            Only return magicasts created before this time (RFC-3339).

        ascending : typing.Optional[bool]
            Sort oldest-first instead of newest-first.

        limit : typing.Optional[int]
            Number of magicasts to return per response. Default 10.

        cursor : typing.Optional[str]
            Opaque pagination cursor from a previous response's `nextCursor`. Do not construct cursors manually.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListMagicastResponse
            Magicasts retrieved successfully

        Examples
        --------
        import asyncio

        from roamhq import AsyncRoamClient

        client = AsyncRoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.magicast.list()


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            after=after, before=before, ascending=ascending, limit=limit, cursor=cursor, request_options=request_options
        )
        return _response.data

    async def info(self, *, id: str, request_options: typing.Optional[RequestOptions] = None) -> MagicastInfo:
        """
        Get details for a single Magicast by ID, including transcript cues,
        chapters, video status, a signed video download URL when ready, and a
        player URL if a share link already exists.

        This is the content endpoint. [`/magicast.list`](https://developer.ro.am/docs/api/magicast-list)
        returns metadata only. Magicasts are not meetings — they do not appear on
        [`/recording.list`](https://developer.ro.am/docs/api/recording-list) or meeting transcript
        surfaces, and they have no Magic Minutes summary or action items.

        Asset, transcript, and share-link lookups are best-effort. If the video or
        transcript is still processing, those fields are omitted and the request
        still succeeds. Fetching this endpoint **never** mints a shareable link;
        use [`/magicast.shareLink`](https://developer.ro.am/docs/api/magicast-share-link) for that.

        There is no `https://ro.am/magicast/{id}` browser URL. The player URL is
        always `https://ro.am/share/{key}`.

        **Access:** Organization and Personal. Organization tokens can read every
        Magicast in the account, including ones the creator never shared. Personal
        tokens are restricted to Magicasts owned by the authenticated user. Filter
        on whether `shareUrl` is present if you only want shared recordings.

        **Required scope:** `magicast:read`

        Parameters
        ----------
        id : str
            The magicast ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MagicastInfo
            Magicast retrieved successfully

        Examples
        --------
        import asyncio

        from roamhq import AsyncRoamClient

        client = AsyncRoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.magicast.info(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.info(id=id, request_options=request_options)
        return _response.data
