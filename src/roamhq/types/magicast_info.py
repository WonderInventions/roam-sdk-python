# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2
from ..core.serialization import FieldMetadata
from .magicast import Magicast
from .magicast_chapter import MagicastChapter
from .magicast_cue import MagicastCue
from .magicast_info_video_status import MagicastInfoVideoStatus


class MagicastInfo(Magicast):
    duration_ms: typing_extensions.Annotated[
        typing.Optional[int],
        FieldMetadata(alias="durationMs"),
        pydantic.Field(
            alias="durationMs", description="Duration of the playable video in milliseconds. Omitted when unknown."
        ),
    ] = None
    """
    Duration of the playable video in milliseconds. Omitted when unknown.
    """

    video_status: typing_extensions.Annotated[
        typing.Optional[MagicastInfoVideoStatus],
        FieldMetadata(alias="videoStatus"),
        pydantic.Field(
            alias="videoStatus",
            description="Where the Magicast video is:\n\n- `none` — no content file. There is nothing to play.\n- `processing` — a content file exists but is not ready yet. Call\n  `/magicast.info` again shortly. You can still mint a share link\n  with [`/magicast.shareLink`](https://developer.ro.am/docs/api/magicast-share-link).\n- `available` — the video is ready. `videoUrl` is a short-lived\n  signed download URL; fetch it at download time and do not persist\n  it as the canonical link.",
        ),
    ] = None
    """
    Where the Magicast video is:
    
    - `none` — no content file. There is nothing to play.
    - `processing` — a content file exists but is not ready yet. Call
      `/magicast.info` again shortly. You can still mint a share link
      with [`/magicast.shareLink`](https://developer.ro.am/docs/api/magicast-share-link).
    - `available` — the video is ready. `videoUrl` is a short-lived
      signed download URL; fetch it at download time and do not persist
      it as the canonical link.
    """

    video_url: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="videoUrl"),
        pydantic.Field(
            alias="videoUrl",
            description="Short-lived signed URL for the video file. Present only when\n`videoStatus` is `available` and the signed URL could be minted.\nDo not persist this URL.",
        ),
    ] = None
    """
    Short-lived signed URL for the video file. Present only when
    `videoStatus` is `available` and the signed URL could be minted.
    Do not persist this URL.
    """

    share_url: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="shareUrl"),
        pydantic.Field(
            alias="shareUrl",
            description="Player URL (`https://ro.am/share/{key}`) if a share link already\nexists. Omitted when nobody has minted one. Fetching this endpoint\nnever creates a share link — use\n[`/magicast.shareLink`](https://developer.ro.am/docs/api/magicast-share-link) for that.\nThere is no `https://ro.am/magicast/{id}` URL.",
        ),
    ] = None
    """
    Player URL (`https://ro.am/share/{key}`) if a share link already
    exists. Omitted when nobody has minted one. Fetching this endpoint
    never creates a share link — use
    [`/magicast.shareLink`](https://developer.ro.am/docs/api/magicast-share-link) for that.
    There is no `https://ro.am/magicast/{id}` URL.
    """

    chapters: typing.Optional[typing.List[MagicastChapter]] = pydantic.Field(default=None)
    """
    Navigation chapters. Omitted when none have been generated.
    """

    cues: typing.Optional[typing.List[MagicastCue]] = pydantic.Field(default=None)
    """
    Flattened transcript sentences. Omitted while the transcript is still
    processing or unavailable. Magicasts are not meetings — there is no
    summary or action-items field.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
