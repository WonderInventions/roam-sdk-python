# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class Sender(UniversalBaseModel):
    """
    Optional sender customization — see the
    [Sender Profiles guide](https://developer.ro.am/docs/guides/sender-profiles).

    `name` / `imageUrl` are **per-message display overrides**: they are stored on
    the message itself and never modify your app's (or a persona's) profile.
    `id` selects a **configured bot persona** (Roam Administration > Developer >
    edit your app > Add Bot Persona) as the message author; an id that doesn't
    match a configured persona is accepted and ignored, and the message is
    authored by the app's root identity.

    Personal access tokens reject this field (400).
    """

    id: typing.Optional[str] = pydantic.Field(default=None)
    """
    Code of a configured bot persona to author the message as (trimmed,
    case-insensitive). Omitted, empty, or `_` posts as the app's root
    identity. Unconfigured ids are accepted and ignored — supplying an id
    never creates a persona.
    """

    name: typing.Optional[str] = pydantic.Field(default=None)
    """
    Display name override for this message only (max 128 characters).
    Does not rename the app or persona.
    """

    image_url: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="imageUrl"),
        pydantic.Field(
            alias="imageUrl",
            description='Avatar URL override for this message only. Must be an absolute HTTP(S)\nURL. From API version `2026-08-25` it must be a Roam-hosted avatar URL\nfrom [`/asset.create`](https://developer.ro.am/docs/api/asset-create) with `purpose: "avatar"`\n(the response `imageUrl`), or a legacy `/card-images/` or\n`/photos/people/` URL. Third-party image URLs return 400. Older version\npins still accept any absolute HTTP(S) URL.',
        ),
    ] = None
    """
    Avatar URL override for this message only. Must be an absolute HTTP(S)
    URL. From API version `2026-08-25` it must be a Roam-hosted avatar URL
    from [`/asset.create`](https://developer.ro.am/docs/api/asset-create) with `purpose: "avatar"`
    (the response `imageUrl`), or a legacy `/card-images/` or
    `/photos/people/` URL. Third-party image URLs return 400. Older version
    pins still accept any absolute HTTP(S) URL.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
