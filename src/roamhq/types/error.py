# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class Error(UniversalBaseModel):
    ok: bool = pydantic.Field()
    """
    Always `false` on error responses.
    """

    error: str = pydantic.Field()
    """
    Machine-readable error code from the catalog
    (e.g. `invalid_token`, `missing_scope`, `ratelimited`, `invalid_cursor`).
    Branch on this field. See [Responses and Errors](https://developer.ro.am/docs/guides/responses-and-errors).
    """

    needed: typing.Optional[typing.List[str]] = pydantic.Field(default=None)
    """
    Present on `missing_scope`. Scopes that would satisfy the check.
    **Any-of (OR)** semantics: holding any one element is enough.
    Distinct from Slack's comma-separated `needed` string.
    """

    provided: typing.Optional[typing.List[str]] = pydantic.Field(default=None)
    """
    Present on `missing_scope`. The token's granted scopes after alias
    normalization (e.g. legacy `groups:read` reports as `group:read`).
    For personal tokens this is the expanded OAuth set, not `pat:*`
    group names.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
