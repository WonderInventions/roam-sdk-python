# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class Reaction(UniversalBaseModel):
    """
    One emoji reaction on a message, grouped across reactors
    (Slack-style `{name, count, users}`).
    """

    name: str = pydantic.Field()
    """
    Reaction shortcode without surrounding colons (e.g. `thumbs_up`, `wave`,
    `heart`). Matches the `name` accepted by `reaction.add` / `reaction.remove`
    and delivered on the `chat.reaction` webhook.
    """

    count: int = pydantic.Field()
    """
    Number of users who added this reaction.
    """

    users: typing.List[str] = pydantic.Field()
    """
    Visible principal IDs of reactors. Hydrate with `user.list?ids`; unauthorized IDs are omitted and `count` reflects this array.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
