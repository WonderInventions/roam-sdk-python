# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import datetime as dt
import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .user_activity_display import UserActivityDisplay


class UserActivity(UniversalBaseModel):
    """
    One live external activity for a user. Returned by `user.activity.set` and
    as each entry in `user.activity.list`.
    """

    user_id: typing_extensions.Annotated[
        str, FieldMetadata(alias="userId"), pydantic.Field(alias="userId", description="Bare UUID of the target user.")
    ]
    """
    Bare UUID of the target user.
    """

    external_id: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="externalId"),
        pydantic.Field(
            alias="externalId",
            description="Caller-chosen id for this session, unique per integration and user\n(for example `justcall:call:CA123`). At most 128 Unicode code points.",
        ),
    ]
    """
    Caller-chosen id for this session, unique per integration and user
    (for example `justcall:call:CA123`). At most 128 Unicode code points.
    """

    display: UserActivityDisplay
    dnd: bool = pydantic.Field()
    """
    Whether this activity currently contributes Do Not Disturb on the
    user's assigned office.
    """

    started_at: typing_extensions.Annotated[
        dt.datetime,
        FieldMetadata(alias="startedAt"),
        pydantic.Field(
            alias="startedAt",
            description="When this session started (RFC3339). A heartbeat that omits\n`startedAt` keeps the original value.",
        ),
    ]
    """
    When this session started (RFC3339). A heartbeat that omits
    `startedAt` keeps the original value.
    """

    expires_at: typing_extensions.Annotated[
        dt.datetime,
        FieldMetadata(alias="expiresAt"),
        pydantic.Field(
            alias="expiresAt",
            description="Server-stamped expiry (RFC3339). The indicator vanishes from the map\nand from `.list` once this instant has passed.",
        ),
    ]
    """
    Server-stamped expiry (RFC3339). The indicator vanishes from the map
    and from `.list` once this instant has passed.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
