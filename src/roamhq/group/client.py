# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.group import Group
from .raw_client import AsyncRawGroupClient, RawGroupClient
from .types.add_group_request_members_item import AddGroupRequestMembersItem
from .types.create_group_request_members_item import CreateGroupRequestMembersItem
from .types.list_group_response import ListGroupResponse
from .types.members_group_response import MembersGroupResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class GroupClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawGroupClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawGroupClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawGroupClient
        """
        return self._raw_client

    def list(
        self,
        *,
        query: typing.Optional[str] = None,
        type: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListGroupResponse:
        """
        Lists non-archived groups accessible to the caller.

        Filter by name with `query` (ranked text match), restrict by group
        type with `type`, and paginate with `limit` / `cursor`.

        **Access:** Organization and Personal.

        **Required scope:** `group:read`

        Parameters
        ----------
        query : typing.Optional[str]
            Text filter. Groups are ranked by how well their name matches the query.

        type : typing.Optional[str]
            Comma-separated list of group types to include. Must be one or
            more of `standard`, `magicast`, `meeting`, `roam`, `onair`.
            Defaults to all types.

        limit : typing.Optional[int]
            Number of groups to return per page (default 50, max 100).

        cursor : typing.Optional[str]
            Opaque pagination cursor from a previous response's `nextCursor`. Do not construct cursors manually.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListGroupResponse
            Groups retrieved successfully.

        Examples
        --------
        from roamhq import RoamClient

        client = RoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )
        client.group.list()
        """
        _response = self._raw_client.list(
            query=query, type=type, limit=limit, cursor=cursor, request_options=request_options
        )
        return _response.data

    def info(
        self,
        *,
        id: typing.Optional[str] = None,
        name: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Group:
        """
        Get information about a specific group by its ID or name.

        Provide either `id` or `name`, not both.

        **Required scope:** `group:read`

        Parameters
        ----------
        id : typing.Optional[str]
            The group's ID. Mutually exclusive with `name`.

        name : typing.Optional[str]
            The group's name. Mutually exclusive with `id`. Returns first match if multiple groups have the same name.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Group
            Group info retrieved successfully

        Examples
        --------
        from roamhq import RoamClient

        client = RoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )
        client.group.info()
        """
        _response = self._raw_client.info(id=id, name=name, request_options=request_options)
        return _response.data

    def create(
        self,
        *,
        name: str,
        members: typing.Sequence[CreateGroupRequestMembersItem],
        description: typing.Optional[str] = OMIT,
        private: typing.Optional[bool] = OMIT,
        enforce_threads: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Group:
        """
        Create a group chat.

        Groups which specify at least one admin will operate in an "Admin only" management
        mode, where only admins may change settings. Otherwise, all members have
        that capability.

        Groups require at least one member. Users can be specified by user ID or email address.

        **Required scope:** `group:write`

        Parameters
        ----------
        name : str
            Name of the group

        members : typing.Sequence[CreateGroupRequestMembersItem]
            Group members with their roles

        description : typing.Optional[str]
            Description of the group

        private : typing.Optional[bool]
            Whether the group is private (default false)

        enforce_threads : typing.Optional[bool]
            Whether to enforce threaded conversations

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Group
            Group created successfully

        Examples
        --------
        from roamhq import RoamClient
        from roamhq.group import CreateGroupRequestMembersItem

        client = RoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )
        client.group.create(
            name="Engineering Team",
            description="Group chat for engineering discussions and updates",
            private=False,
            enforce_threads=True,
            members=[
                CreateGroupRequestMembersItem(
                    user_id="alex.chen@example.com",
                    role="member",
                ),
                CreateGroupRequestMembersItem(
                    user_id="taylor@example.com",
                    role="member",
                ),
                CreateGroupRequestMembersItem(
                    user_id="jordan.smith@example.com",
                    role="admin",
                ),
            ],
        )
        """
        _response = self._raw_client.create(
            name=name,
            members=members,
            description=description,
            private=private,
            enforce_threads=enforce_threads,
            request_options=request_options,
        )
        return _response.data

    def rename(self, *, id: str, name: str, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Rename a group by ID.

        Apps may only rename groups for which they are an admin.

        **Required scope:** `group:write`

        Parameters
        ----------
        id : str
            The group ID

        name : str
            The new name for the group

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from roamhq import RoamClient

        client = RoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )
        client.group.rename(
            id="88bebce7-6cbb-4666-96f9-5c02d73e6661",
            name="Product Engineering",
        )
        """
        _response = self._raw_client.rename(id=id, name=name, request_options=request_options)
        return _response.data

    def archive(self, *, id: str, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Archive a group by ID.

        Apps may only archive groups for which they are an admin.

        **Required scope:** `group:write`

        Parameters
        ----------
        id : str
            The group ID to archive.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from roamhq import RoamClient

        client = RoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )
        client.group.archive(
            id="88bebce7-6cbb-4666-96f9-5c02d73e6661",
        )
        """
        _response = self._raw_client.archive(id=id, request_options=request_options)
        return _response.data

    def members(
        self,
        *,
        id: str,
        limit: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MembersGroupResponse:
        """
        List members in a group with their roles.

        Apps may list members if one of the following conditions is true:
        1. It is a public group in their Roam.
        2. They are a member of the group.

        **Required scope:** `group:read`

        Every returned `userId` is a visible principal ID that resolves through
        [`user.info`](https://developer.ro.am/docs/api/user-info) with the same credentials. Use
        `user.list?ids` for ordered bulk hydration.

        Parameters
        ----------
        id : str
            Group ID.

        limit : typing.Optional[int]
            The number of members to return per response. Default is 10.

        cursor : typing.Optional[str]
            Opaque pagination cursor from a previous response's `nextCursor`. Do not construct cursors manually.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MembersGroupResponse
            Members retrieved successfully

        Examples
        --------
        from roamhq import RoamClient

        client = RoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )
        client.group.members(
            id="id",
        )
        """
        _response = self._raw_client.members(id=id, limit=limit, cursor=cursor, request_options=request_options)
        return _response.data

    def add(
        self,
        *,
        id: str,
        members: typing.Optional[typing.Sequence[AddGroupRequestMembersItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Add one or more group members with specified roles.

        Members can be specified by user ID or email address. Each member must be assigned a role (member or admin).

        Apps may add members to a group if one of the following conditions is true:
        1. It is a public group in their Roam.
        2. They are a member of the group.

        If attempting to add an admin, the app must be an admin of the group.

        **Required scope:** `group:write`

        Parameters
        ----------
        id : str
            Group ID

        members : typing.Optional[typing.Sequence[AddGroupRequestMembersItem]]
            List of members to add with their roles

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from roamhq import RoamClient
        from roamhq.group import AddGroupRequestMembersItem

        client = RoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )
        client.group.add(
            id="88bebce7-6cbb-4666-96f9-5c02d73e6661",
            members=[
                AddGroupRequestMembersItem(
                    user_id="709b8a57-70bc-427a-b6f0-b16ba5297f8c",
                    role="member",
                ),
                AddGroupRequestMembersItem(
                    user_id="f589a8cb-78ac-493e-8719-0fa8a22f65e0",
                    role="member",
                ),
                AddGroupRequestMembersItem(
                    user_id="af6663d5-0f37-4105-95df-4fea20ef7c7c",
                    role="admin",
                ),
            ],
        )
        """
        _response = self._raw_client.add(id=id, members=members, request_options=request_options)
        return _response.data

    def join(self, *, id: str, request_options: typing.Optional[RequestOptions] = None) -> Group:
        """
        Join a public group as the calling identity (Slack `conversations.join`).

        - Org tokens add the bot address as a member.
        - Personal tokens add the **owner person**, never the PAT bot address.
        - Private groups cannot be self-joined (`403`).
        - Idempotent if the calling identity is already a member.
        - Non-members of a group in another roam receive an opaque `403`
          (`group_not_found`) — archived / type / privacy are not distinguished.

        Why join (webhooks vs history vs post): [Chat](https://developer.ro.am/docs/guides/chat).

        **Access:** Organization and Personal.

        **Required scope:** `group:write`

        Parameters
        ----------
        id : str
            Group ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Group
            Joined the group (or already a member)

        Examples
        --------
        from roamhq import RoamClient

        client = RoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )
        client.group.join(
            id="88bebce7-6cbb-4666-96f9-5c02d73e6661",
        )
        """
        _response = self._raw_client.join(id=id, request_options=request_options)
        return _response.data

    def remove(
        self, *, id: str, members: typing.Sequence[str], request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Remove one or more group members.

        Members can be specified by user ID or email address.

        Apps may remove members from a group if one of the following conditions is true:
        1. It is a public group in their Roam.
        2. They are a member of the group.

        Removing members with the Admin role is not yet supported.

        **Required scope:** `group:write`

        Parameters
        ----------
        id : str
            Group ID

        members : typing.Sequence[str]
            List of member IDs or email addresses to remove

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from roamhq import RoamClient

        client = RoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )
        client.group.remove(
            id="88bebce7-6cbb-4666-96f9-5c02d73e6661",
            members=["709b8a57-70bc-427a-b6f0-b16ba5297f8c"],
        )
        """
        _response = self._raw_client.remove(id=id, members=members, request_options=request_options)
        return _response.data


class AsyncGroupClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawGroupClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawGroupClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawGroupClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        query: typing.Optional[str] = None,
        type: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListGroupResponse:
        """
        Lists non-archived groups accessible to the caller.

        Filter by name with `query` (ranked text match), restrict by group
        type with `type`, and paginate with `limit` / `cursor`.

        **Access:** Organization and Personal.

        **Required scope:** `group:read`

        Parameters
        ----------
        query : typing.Optional[str]
            Text filter. Groups are ranked by how well their name matches the query.

        type : typing.Optional[str]
            Comma-separated list of group types to include. Must be one or
            more of `standard`, `magicast`, `meeting`, `roam`, `onair`.
            Defaults to all types.

        limit : typing.Optional[int]
            Number of groups to return per page (default 50, max 100).

        cursor : typing.Optional[str]
            Opaque pagination cursor from a previous response's `nextCursor`. Do not construct cursors manually.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListGroupResponse
            Groups retrieved successfully.

        Examples
        --------
        import asyncio

        from roamhq import AsyncRoamClient

        client = AsyncRoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.group.list()


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            query=query, type=type, limit=limit, cursor=cursor, request_options=request_options
        )
        return _response.data

    async def info(
        self,
        *,
        id: typing.Optional[str] = None,
        name: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Group:
        """
        Get information about a specific group by its ID or name.

        Provide either `id` or `name`, not both.

        **Required scope:** `group:read`

        Parameters
        ----------
        id : typing.Optional[str]
            The group's ID. Mutually exclusive with `name`.

        name : typing.Optional[str]
            The group's name. Mutually exclusive with `id`. Returns first match if multiple groups have the same name.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Group
            Group info retrieved successfully

        Examples
        --------
        import asyncio

        from roamhq import AsyncRoamClient

        client = AsyncRoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.group.info()


        asyncio.run(main())
        """
        _response = await self._raw_client.info(id=id, name=name, request_options=request_options)
        return _response.data

    async def create(
        self,
        *,
        name: str,
        members: typing.Sequence[CreateGroupRequestMembersItem],
        description: typing.Optional[str] = OMIT,
        private: typing.Optional[bool] = OMIT,
        enforce_threads: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Group:
        """
        Create a group chat.

        Groups which specify at least one admin will operate in an "Admin only" management
        mode, where only admins may change settings. Otherwise, all members have
        that capability.

        Groups require at least one member. Users can be specified by user ID or email address.

        **Required scope:** `group:write`

        Parameters
        ----------
        name : str
            Name of the group

        members : typing.Sequence[CreateGroupRequestMembersItem]
            Group members with their roles

        description : typing.Optional[str]
            Description of the group

        private : typing.Optional[bool]
            Whether the group is private (default false)

        enforce_threads : typing.Optional[bool]
            Whether to enforce threaded conversations

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Group
            Group created successfully

        Examples
        --------
        import asyncio

        from roamhq import AsyncRoamClient
        from roamhq.group import CreateGroupRequestMembersItem

        client = AsyncRoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.group.create(
                name="Engineering Team",
                description="Group chat for engineering discussions and updates",
                private=False,
                enforce_threads=True,
                members=[
                    CreateGroupRequestMembersItem(
                        user_id="alex.chen@example.com",
                        role="member",
                    ),
                    CreateGroupRequestMembersItem(
                        user_id="taylor@example.com",
                        role="member",
                    ),
                    CreateGroupRequestMembersItem(
                        user_id="jordan.smith@example.com",
                        role="admin",
                    ),
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            name=name,
            members=members,
            description=description,
            private=private,
            enforce_threads=enforce_threads,
            request_options=request_options,
        )
        return _response.data

    async def rename(self, *, id: str, name: str, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Rename a group by ID.

        Apps may only rename groups for which they are an admin.

        **Required scope:** `group:write`

        Parameters
        ----------
        id : str
            The group ID

        name : str
            The new name for the group

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from roamhq import AsyncRoamClient

        client = AsyncRoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.group.rename(
                id="88bebce7-6cbb-4666-96f9-5c02d73e6661",
                name="Product Engineering",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.rename(id=id, name=name, request_options=request_options)
        return _response.data

    async def archive(self, *, id: str, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Archive a group by ID.

        Apps may only archive groups for which they are an admin.

        **Required scope:** `group:write`

        Parameters
        ----------
        id : str
            The group ID to archive.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from roamhq import AsyncRoamClient

        client = AsyncRoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.group.archive(
                id="88bebce7-6cbb-4666-96f9-5c02d73e6661",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.archive(id=id, request_options=request_options)
        return _response.data

    async def members(
        self,
        *,
        id: str,
        limit: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MembersGroupResponse:
        """
        List members in a group with their roles.

        Apps may list members if one of the following conditions is true:
        1. It is a public group in their Roam.
        2. They are a member of the group.

        **Required scope:** `group:read`

        Every returned `userId` is a visible principal ID that resolves through
        [`user.info`](https://developer.ro.am/docs/api/user-info) with the same credentials. Use
        `user.list?ids` for ordered bulk hydration.

        Parameters
        ----------
        id : str
            Group ID.

        limit : typing.Optional[int]
            The number of members to return per response. Default is 10.

        cursor : typing.Optional[str]
            Opaque pagination cursor from a previous response's `nextCursor`. Do not construct cursors manually.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MembersGroupResponse
            Members retrieved successfully

        Examples
        --------
        import asyncio

        from roamhq import AsyncRoamClient

        client = AsyncRoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.group.members(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.members(id=id, limit=limit, cursor=cursor, request_options=request_options)
        return _response.data

    async def add(
        self,
        *,
        id: str,
        members: typing.Optional[typing.Sequence[AddGroupRequestMembersItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Add one or more group members with specified roles.

        Members can be specified by user ID or email address. Each member must be assigned a role (member or admin).

        Apps may add members to a group if one of the following conditions is true:
        1. It is a public group in their Roam.
        2. They are a member of the group.

        If attempting to add an admin, the app must be an admin of the group.

        **Required scope:** `group:write`

        Parameters
        ----------
        id : str
            Group ID

        members : typing.Optional[typing.Sequence[AddGroupRequestMembersItem]]
            List of members to add with their roles

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from roamhq import AsyncRoamClient
        from roamhq.group import AddGroupRequestMembersItem

        client = AsyncRoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.group.add(
                id="88bebce7-6cbb-4666-96f9-5c02d73e6661",
                members=[
                    AddGroupRequestMembersItem(
                        user_id="709b8a57-70bc-427a-b6f0-b16ba5297f8c",
                        role="member",
                    ),
                    AddGroupRequestMembersItem(
                        user_id="f589a8cb-78ac-493e-8719-0fa8a22f65e0",
                        role="member",
                    ),
                    AddGroupRequestMembersItem(
                        user_id="af6663d5-0f37-4105-95df-4fea20ef7c7c",
                        role="admin",
                    ),
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.add(id=id, members=members, request_options=request_options)
        return _response.data

    async def join(self, *, id: str, request_options: typing.Optional[RequestOptions] = None) -> Group:
        """
        Join a public group as the calling identity (Slack `conversations.join`).

        - Org tokens add the bot address as a member.
        - Personal tokens add the **owner person**, never the PAT bot address.
        - Private groups cannot be self-joined (`403`).
        - Idempotent if the calling identity is already a member.
        - Non-members of a group in another roam receive an opaque `403`
          (`group_not_found`) — archived / type / privacy are not distinguished.

        Why join (webhooks vs history vs post): [Chat](https://developer.ro.am/docs/guides/chat).

        **Access:** Organization and Personal.

        **Required scope:** `group:write`

        Parameters
        ----------
        id : str
            Group ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Group
            Joined the group (or already a member)

        Examples
        --------
        import asyncio

        from roamhq import AsyncRoamClient

        client = AsyncRoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.group.join(
                id="88bebce7-6cbb-4666-96f9-5c02d73e6661",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.join(id=id, request_options=request_options)
        return _response.data

    async def remove(
        self, *, id: str, members: typing.Sequence[str], request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Remove one or more group members.

        Members can be specified by user ID or email address.

        Apps may remove members from a group if one of the following conditions is true:
        1. It is a public group in their Roam.
        2. They are a member of the group.

        Removing members with the Admin role is not yet supported.

        **Required scope:** `group:write`

        Parameters
        ----------
        id : str
            Group ID

        members : typing.Sequence[str]
            List of member IDs or email addresses to remove

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from roamhq import AsyncRoamClient

        client = AsyncRoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.group.remove(
                id="88bebce7-6cbb-4666-96f9-5c02d73e6661",
                members=["709b8a57-70bc-427a-b6f0-b16ba5297f8c"],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.remove(id=id, members=members, request_options=request_options)
        return _response.data
