# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from .raw_client import AsyncRawTokenClient, RawTokenClient
from .types.info_token_response import InfoTokenResponse


class TokenClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawTokenClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawTokenClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawTokenClient
        """
        return self._raw_client

    def info(self, *, request_options: typing.Optional[RequestOptions] = None) -> InfoTokenResponse:
        """
        Get information about the access token, including the authenticated user/bot
        and granted scopes.

        **No specific scope required.**

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        InfoTokenResponse
            Token info retrieved successfully

        Examples
        --------
        from roamhq import RoamClient

        client = RoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )
        client.token.info()
        """
        _response = self._raw_client.info(request_options=request_options)
        return _response.data

    def revoke(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Permanently revoke the presented OAuth access token **and its refresh
        token**. After a successful response the grant is dead — refresh will not
        resurrect it; the client must re-authorize.

        This does **not** uninstall your app from the workspace, delete webhook
        subscriptions, or affect other users' tokens. For install removal see the
        [`app.uninstalled`](https://developer.ro.am/docs/webhooks/app-uninstalled) event (fired from admin
        / Dev Settings uninstall paths, not from this endpoint).

        On success Roam also delivers a [`token.revoked`](https://developer.ro.am/docs/webhooks/token-revoked)
        webhook to your subscriptions (`reason: "api_revoked"`), including to the
        same app that called this endpoint. Treat that delivery as idempotent.

        Subsequent API calls with the revoked access token return HTTP `401` with
        `invalid_token` (the token row is gone). Distinct from `token_revoked`,
        which signals an archived person or archived client while a credential may
        still exist.

        This operation is only valid for OAuth access tokens, not for API keys.

        **Access:** Organization and Personal (OAuth access tokens only). Personal
        tokens may revoke their own grant. API keys cannot use this endpoint.

        **No specific scope required.**

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from roamhq import RoamClient

        client = RoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )
        client.token.revoke()
        """
        _response = self._raw_client.revoke(request_options=request_options)
        return _response.data


class AsyncTokenClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawTokenClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawTokenClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawTokenClient
        """
        return self._raw_client

    async def info(self, *, request_options: typing.Optional[RequestOptions] = None) -> InfoTokenResponse:
        """
        Get information about the access token, including the authenticated user/bot
        and granted scopes.

        **No specific scope required.**

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        InfoTokenResponse
            Token info retrieved successfully

        Examples
        --------
        import asyncio

        from roamhq import AsyncRoamClient

        client = AsyncRoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.token.info()


        asyncio.run(main())
        """
        _response = await self._raw_client.info(request_options=request_options)
        return _response.data

    async def revoke(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Permanently revoke the presented OAuth access token **and its refresh
        token**. After a successful response the grant is dead — refresh will not
        resurrect it; the client must re-authorize.

        This does **not** uninstall your app from the workspace, delete webhook
        subscriptions, or affect other users' tokens. For install removal see the
        [`app.uninstalled`](https://developer.ro.am/docs/webhooks/app-uninstalled) event (fired from admin
        / Dev Settings uninstall paths, not from this endpoint).

        On success Roam also delivers a [`token.revoked`](https://developer.ro.am/docs/webhooks/token-revoked)
        webhook to your subscriptions (`reason: "api_revoked"`), including to the
        same app that called this endpoint. Treat that delivery as idempotent.

        Subsequent API calls with the revoked access token return HTTP `401` with
        `invalid_token` (the token row is gone). Distinct from `token_revoked`,
        which signals an archived person or archived client while a credential may
        still exist.

        This operation is only valid for OAuth access tokens, not for API keys.

        **Access:** Organization and Personal (OAuth access tokens only). Personal
        tokens may revoke their own grant. API keys cannot use this endpoint.

        **No specific scope required.**

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from roamhq import AsyncRoamClient

        client = AsyncRoamClient(
            roam_version="YOUR_ROAM_VERSION",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.token.revoke()


        asyncio.run(main())
        """
        _response = await self._raw_client.revoke(request_options=request_options)
        return _response.data
