# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import datetime as dt
import typing

import pydantic
import typing_extensions
from ...core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ...core.serialization import FieldMetadata
from ...types.action_item import ActionItem
from ...types.meeting_participant import MeetingParticipant
from .info_meeting_response_chapters_item import InfoMeetingResponseChaptersItem
from .info_meeting_response_video_status import InfoMeetingResponseVideoStatus


class InfoMeetingResponse(UniversalBaseModel):
    id: str = pydantic.Field()
    """
    Meeting ID
    """

    title: str = pydantic.Field()
    """
    Meeting title
    """

    subtitle: typing.Optional[str] = pydantic.Field(default=None)
    """
    Meeting subtitle
    """

    start: dt.datetime = pydantic.Field()
    """
    Meeting start time (RFC-3339)
    """

    end: typing.Optional[dt.datetime] = pydantic.Field(default=None)
    """
    Meeting end time (RFC-3339). Omitted while the meeting is still in progress.
    """

    participant_count: typing_extensions.Annotated[
        int,
        FieldMetadata(alias="participantCount"),
        pydantic.Field(alias="participantCount", description="Total number of participants"),
    ]
    """
    Total number of participants
    """

    has_video: typing_extensions.Annotated[
        bool,
        FieldMetadata(alias="hasVideo"),
        pydantic.Field(
            alias="hasVideo",
            description='Whether the meeting was video recorded — a video track exists.\n`true` from the moment recording starts, `true` at meeting end, and\nit never flips back. It does **not** mean the recording is ready to\nfetch or play; read `videoStatus` for that. Matches `meeting.list`,\nthe `meeting.ended` webhook `data.hasVideo`, and the\n`{"hasVideo": true}` subscription filter.',
        ),
    ]
    """
    Whether the meeting was video recorded — a video track exists.
    `true` from the moment recording starts, `true` at meeting end, and
    it never flips back. It does **not** mean the recording is ready to
    fetch or play; read `videoStatus` for that. Matches `meeting.list`,
    the `meeting.ended` webhook `data.hasVideo`, and the
    `{"hasVideo": true}` subscription filter.
    """

    video_status: typing_extensions.Annotated[
        InfoMeetingResponseVideoStatus,
        FieldMetadata(alias="videoStatus"),
        pydantic.Field(
            alias="videoStatus",
            description="Where the meeting's video recording is, which — unlike `hasVideo` —\nchanges over time:\n\n- `none` — no video track. The meeting was not recorded and no\n  recording will appear later. Always paired with `hasVideo: false`.\n- `processing` — a recording exists but its upload has not finished,\n  so there is nothing to play yet. Call `/meeting.info` again\n  shortly.\n- `available` — the recording is uploaded and has an asset. Use\n  [`/meeting.shareLink`](https://developer.ro.am/docs/api/meeting-share-link) to get a\n  shareable link to it.\n\nNot present on the `meeting.ended` webhook payload — see that\nevent's page.",
        ),
    ]
    """
    Where the meeting's video recording is, which — unlike `hasVideo` —
    changes over time:
    
    - `none` — no video track. The meeting was not recorded and no
      recording will appear later. Always paired with `hasVideo: false`.
    - `processing` — a recording exists but its upload has not finished,
      so there is nothing to play yet. Call `/meeting.info` again
      shortly.
    - `available` — the recording is uploaded and has an asset. Use
      [`/meeting.shareLink`](https://developer.ro.am/docs/api/meeting-share-link) to get a
      shareable link to it.
    
    Not present on the `meeting.ended` webhook payload — see that
    event's page.
    """

    host: typing.Optional[MeetingParticipant] = pydantic.Field(default=None)
    """
    Meeting host as a participant object. Requires `user:read`
    scope; emails are only included with `user:read.email`.
    Omitted when the host cannot be resolved.
    """

    participants: typing.Optional[typing.List[MeetingParticipant]] = pydantic.Field(default=None)
    """
    Resolved participants (up to `maxParticipants`). Requires `user:read` scope.
    """

    participants_omitted: typing_extensions.Annotated[
        typing.Optional[bool],
        FieldMetadata(alias="participantsOmitted"),
        pydantic.Field(
            alias="participantsOmitted", description="True if total participants exceeds the resolved count"
        ),
    ] = None
    """
    True if total participants exceeds the resolved count
    """

    summary: typing.Optional[str] = pydantic.Field(default=None)
    """
    AI-generated meeting summary
    """

    action_items: typing_extensions.Annotated[
        typing.Optional[typing.List[ActionItem]],
        FieldMetadata(alias="actionItems"),
        pydantic.Field(alias="actionItems", description="AI-extracted action items"),
    ] = None
    """
    AI-extracted action items
    """

    chapters: typing.Optional[typing.List[InfoMeetingResponseChaptersItem]] = pydantic.Field(default=None)
    """
    Meeting chapters/segments
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
